{
  "name": "gentyr",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hook dependencies for GENTYR",
  "scripts": {
    "test:e2e": "vitest run --config tests/e2e/vitest.config.ts",
    "generate:readme": "node scripts/generate-readme.js"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "better-sqlite3": "^12.6.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
